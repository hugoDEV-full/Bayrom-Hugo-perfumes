<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Bayrom & Hugo Parfums' %></title>
    <meta name="description" content="<%= typeof description !== 'undefined' ? description : 'Loja virtual de perfumes premium - Bayrom & Hugo Parfums' %>">
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;900&family=Montserrat:wght@300;400;500;600;700&family=Cormorant+Garamond:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/luxury.css">
    <link rel="stylesheet" href="/css/luxury-materials.css">
    <link rel="stylesheet" href="/css/material-extensions.css">
</head>
<body>

    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="/">
                <strong>Bayrom & Hugo Parfums</strong>
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><i class="fas fa-house"></i> Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/products"><i class="fas fa-spray-can"></i> Produtos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/cart"><i class="fas fa-cart-shopping"></i> Carrinho</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/checkout"><i class="fas fa-credit-card"></i> Finalizar compra</a>
                    </li>
                    <li class="nav-item">
                        <div class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false"><i class="fas fa-circle-info"></i> Conteúdo</a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/products/inspiracoes"><i class="fas fa-wand-magic-sparkles"></i> Catálogo de Inspirações</a></li>
                                <li><a class="dropdown-item" href="/encyclopedia"><i class="fas fa-book-open"></i> Enciclopédia</a></li>
                                <li><a class="dropdown-item" href="/como-comprar"><i class="fas fa-shopping-bag"></i> Como comprar</a></li>
                                <li><a class="dropdown-item" href="/materiais-preciosos"><i class="fas fa-gem"></i> Materiais Preciosos</a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/sobre-nos"><i class="fas fa-info-circle"></i> Sobre nós</a></li>
                                <li><a class="dropdown-item" href="/contato"><i class="fas fa-envelope"></i> Contato</a></li>
                            </ul>
                        </div>
                    </li>
                </ul>
                
                <div class="d-flex align-items-center">
                    <% if (typeof user !== 'undefined' && user) { %>
                        <div class="dropdown">
                            <button class="btn btn-link text-dark dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                <i class="fas fa-user"></i>
                                <span class="d-none d-md-inline"><%= user.name %></span>
                            </button>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/profile"><i class="fas fa-user"></i> Meu Perfil</a></li>
                                <li><a class="dropdown-item" href="/orders"><i class="fas fa-box"></i> Meus Pedidos</a></li>
                                <% if (user.role === 'admin') { %>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/admin"><i class="fas fa-cog"></i> Painel Admin</a></li>
                                <% } %>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/auth/logout"><i class="fas fa-sign-out-alt"></i> Sair</a></li>
                            </ul>
                        </div>
                    <% } else { %>
                        <a href="/auth/login" class="btn btn-link text-dark">
                            <i class="fas fa-user"></i>
                            <span class="d-none d-md-inline">Entrar</span>
                        </a>
                    <% } %>
                    
                    <!-- Cart -->
                    <a href="/cart" class="btn btn-link text-dark position-relative ms-3">
                        <i class="fas fa-shopping-cart"></i>
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            <%= (typeof cart !== 'undefined' && cart) ? cart.reduce((sum, item) => sum + item.quantity, 0) : 0 %>
                        </span>
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
    <% if (typeof success_msg !== 'undefined' && success_msg.length > 0) { %>
        <div class="alert alert-success alert-dismissible fade show m-0" role="alert">
            <%= success_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>
    
    <% if (typeof error_msg !== 'undefined' && error_msg.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show m-0" role="alert">
            <%= error_msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    <% } %>

    <!-- Main Content -->
